extensions:
    - PhpSpec\Extension\CodeCoverageExtension

suites:
  types:
    namespace: WebHookEvents\RepositoryEvents
    psr4_prefix: WebHookEvents\RepositoryEvents
    spec_path: tests

code_coverage:
    output: tests/coverage/phpspec.cov
    format: php
